<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{business_name} | Social Influencer</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Righteous&family=Outfit:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        :root {
            --bg: #fffef9;
            --text: #1a1a1a;
            --pink: #ff6b9d;
            --purple: #c44569;
            --orange: #ff9a76;
            --yellow: #ffc837;
            --blue: #4facfe;
        }
        
        body {
            font-family: 'Outfit', sans-serif;
            background: var(--bg);
            color: var(--text);
            line-height: 1.6;
        }
        
        /* Floating shapes background */
        .bg-shapes {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            overflow: hidden;
        }
        
        .shape {
            position: absolute;
            border-radius: 50%;
            opacity: 0.1;
            animation: float 20s infinite ease-in-out;
        }
        
        .shape:nth-child(1) {
            width: 300px;
            height: 300px;
            background: var(--pink);
            top: 10%;
            left: 10%;
            animation-delay: 0s;
        }
        
        .shape:nth-child(2) {
            width: 200px;
            height: 200px;
            background: var(--blue);
            top: 50%;
            right: 15%;
            animation-delay: 5s;
        }
        
        .shape:nth-child(3) {
            width: 250px;
            height: 250px;
            background: var(--yellow);
            bottom: 10%;
            left: 20%;
            animation-delay: 10s;
        }
        
        @keyframes float {
            0%, 100% { transform: translate(0, 0) rotate(0deg); }
            33% { transform: translate(30px, -30px) rotate(120deg); }
            66% { transform: translate(-20px, 20px) rotate(240deg); }
        }
        
        header {
            padding: 2rem;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            background: rgba(255, 254, 249, 0.9);
            backdrop-filter: blur(10px);
            z-index: 1000;
            border-bottom: 2px solid rgba(0, 0, 0, 0.05);
        }
        
        nav {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .logo {
            font-family: 'Righteous', sans-serif;
            font-size: 1.8rem;
            background: linear-gradient(135deg, var(--pink), var(--purple));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .social-links {
            display: flex;
            gap: 1rem;
            list-style: none;
        }
        
        .social-links a {
            width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--pink), var(--purple));
            color: white;
            text-decoration: none;
            font-size: 1.2rem;
            transition: transform 0.3s;
        }
        
        .social-links a:hover {
            transform: scale(1.1) rotate(5deg);
        }
        
        .hero {
            min-height: 100vh;
            padding: 10rem 2rem 4rem;
            display: flex;
            align-items: center;
        }
        
        .hero-content {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
        }
        
        .hero-text h1 {
            font-family: 'Righteous', sans-serif;
            font-size: clamp(2.5rem, 8vw, 4.5rem);
            line-height: 1.1;
            margin-bottom: 1.5rem;
            background: linear-gradient(135deg, var(--pink), var(--purple), var(--orange));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .hero-text p {
            font-size: 1.3rem;
            color: #666;
            margin-bottom: 2rem;
            line-height: 1.6;
        }
        
        .stats-row {
            display: flex;
            gap: 2rem;
            margin-bottom: 2rem;
        }
        
        .stat-box {
            flex: 1;
            padding: 1.5rem;
            border-radius: 20px;
            background: linear-gradient(135deg, var(--pink) 0%, var(--purple) 100%);
            color: white;
            text-align: center;
        }
        
        .stat-box:nth-child(2) {
            background: linear-gradient(135deg, var(--blue) 0%, var(--purple) 100%);
        }
        
        .stat-box:nth-child(3) {
            background: linear-gradient(135deg, var(--orange) 0%, var(--yellow) 100%);
        }
        
        .stat-number {
            font-family: 'Righteous', sans-serif;
            font-size: 2rem;
            margin-bottom: 0.25rem;
        }
        
        .stat-label {
            font-size: 0.9rem;
            opacity: 0.9;
        }
        
        .btn-group {
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
        }
        
        .btn {
            padding: 1rem 2rem;
            font-size: 1rem;
            font-weight: 600;
            text-decoration: none;
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .btn-primary {
            background: linear-gradient(135deg, var(--pink), var(--purple));
            color: white;
            border: none;
            box-shadow: 0 10px 30px rgba(255, 107, 157, 0.3);
        }
        
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 15px 40px rgba(255, 107, 157, 0.4);
        }
        
        .btn-secondary {
            background: white;
            color: var(--text);
            border: 2px solid var(--text);
        }
        
        .btn-secondary:hover {
            background: var(--text);
            color: white;
        }
        
        .hero-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 1rem;
        }
        
        .grid-item {
            aspect-ratio: 1;
            border-radius: 20px;
            overflow: hidden;
            background: linear-gradient(135deg, var(--pink), var(--purple));
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 3rem;
            color: white;
            transition: transform 0.3s;
        }
        
        .grid-item:hover {
            transform: scale(1.05);
        }
        
        .grid-item:nth-child(1) {
            background: linear-gradient(135deg, var(--pink), var(--purple));
        }
        
        .grid-item:nth-child(2) {
            background: linear-gradient(135deg, var(--blue), var(--purple));
        }
        
        .grid-item:nth-child(3) {
            background: linear-gradient(135deg, var(--orange), var(--yellow));
        }
        
        .grid-item:nth-child(4) {
            background: linear-gradient(135deg, var(--yellow), var(--orange));
        }
        
        .content-section {
            padding: 6rem 2rem;
            background: white;
        }
        
        .section-title {
            font-family: 'Righteous', sans-serif;
            font-size: 3rem;
            text-align: center;
            margin-bottom: 3rem;
            background: linear-gradient(135deg, var(--pink), var(--purple));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .content-grid {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
        }
        
        .content-card {
            padding: 2rem;
            border-radius: 20px;
            background: var(--bg);
            border: 2px solid rgba(0, 0, 0, 0.05);
            transition: all 0.3s;
        }
        
        .content-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        }
        
        .card-icon {
            width: 60px;
            height: 60px;
            border-radius: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            margin-bottom: 1rem;
            background: linear-gradient(135deg, var(--pink), var(--purple));
            color: white;
        }
        
        .content-card h3 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
        }
        
        .content-card p {
            color: #666;
            line-height: 1.6;
        }
        
        footer {
            padding: 3rem 2rem;
            text-align: center;
            background: var(--text);
            color: white;
        }
        
        footer p {
            font-size: 0.95rem;
        }
        
        @media (max-width: 968px) {
            .hero-content {
                grid-template-columns: 1fr;
            }
            
            .social-links {
                display: none;
            }
        }
    </style>
</head>
<body>
    <div class="bg-shapes">
        <div class="shape"></div>
        <div class="shape"></div>
        <div class="shape"></div>
    </div>
    
    <header>
        <nav>
            <div class="logo">{business_name}</div>
            <ul class="social-links">
                <li><a href="#">📸</a></li>
                <li><a href="#">🎵</a></li>
                <li><a href="#">📹</a></li>
            </ul>
        </nav>
    </header>

    <section class="hero">
        <div class="hero-content">
            <div class="hero-text">
                <h1>Creating content that matters ✨</h1>
                <p>{tagline}</p>
                
                <div class="stats-row">
                    <div class="stat-box">
                        <div class="stat-number">500K+</div>
                        <div class="stat-label">Followers</div>
                    </div>
                    <div class="stat-box">
                        <div class="stat-number">10M+</div>
                        <div class="stat-label">Views</div>
                    </div>
                    <div class="stat-box">
                        <div class="stat-number">98%</div>
                        <div class="stat-label">Engagement</div>
                    </div>
                </div>
                
                <div class="btn-group">
                    <a href="mailto:{contact_email}" class="btn btn-primary">Work Together →</a>
                    <a href="#content" class="btn btn-secondary">View Content</a>
                </div>
            </div>
            
            <div class="hero-grid">
                <div class="grid-item">📸</div>
                <div class="grid-item">✨</div>
                <div class="grid-item">💫</div>
                <div class="grid-item">🎨</div>
            </div>
        </div>
    </section>

    <section class="content-section">
        <h2 class="section-title">What I Do</h2>
        
        <div class="content-grid">
            <div class="content-card">
                <div class="card-icon">📱</div>
                <h3>Social Media</h3>
                <p>Daily content across Instagram, TikTok, and YouTube. Building authentic connections with my community.</p>
            </div>
            <div class="content-card">
                <div class="card-icon">🎥</div>
                <h3>Brand Partnerships</h3>
                <p>Collaborating with brands that align with my values and resonate with my audience.</p>
            </div>
            <div class="content-card">
                <div class="card-icon">✨</div>
                <h3>Content Creation</h3>
                <p>Professional photography, videography, and storytelling that drives engagement.</p>
            </div>
        </div>
    </section>

    <footer>
        <p>© 2025 {business_name} • Powered by AiGentsy • {contact_email}</p>
    </footer>
</body>
</html>
