# ═══════════════════════════════════════════════════════════════════════════════
# AiGentsy Autonomous Execution Workflow v93 - APEX UPGRADES + WADE AUTO-APPROVE
# January 4, 2026
# ═══════════════════════════════════════════════════════════════════════════════
#
# INCLUDES ALL ENDPOINTS FROM v92 PLUS:
# - v93 NEW: APEX UPGRADES (12 Revenue Optimization Modules)
#   • Intent Value Router - Bid where ROI > 1
#   • CAC-Aware Pricing - Include true costs in bids  
#   • Upsell/Cross-Sell - Turn single orders into bundles
#   • Abandon Recovery - Recover carts with proof
#   • LTV Forecaster - Prioritize high-rebuy clients
#   • Portfolio Rebalancing - Auto-tune offer mix
#   • Churn Sentinel - Save MRR with targeted offers
#   • Proof Badges - Auto-generate social proof
#
# - v93 NEW: WADE AUTO-APPROVE
#   • /wade/approve-all - One-click approve 1000+ items
#   • /wade/execution-status - See what's running
#
# TOTAL: 152 UNIQUE ENDPOINTS
#
# ═══════════════════════════════════════════════════════════════════════════════

name: AiGentsy v93 Apex Upgrades

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:
    inputs:
      mode:
        description: 'Execution mode'
        required: false
        default: 'full'
        type: choice
        options:
          - full
          - discovery
          - financial
          - marketing
          - health

env:
  BACKEND_URL: ${{ secrets.BACKEND_URL || 'https://aigentsy-ame-runtime.onrender.com' }}

jobs:
  complete-automation:
    name: 🚀 v93 Apex Upgrades (152 endpoints)
    runs-on: ubuntu-latest
    timeout-minutes: 55

    steps:
      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 0: UNIVERSAL REVENUE ORCHESTRATOR (NEW - THE MASTER)
      # ═══════════════════════════════════════════════════════════════════════
      - name: 💰 Universal Revenue Orchestrator (10 endpoints)
        continue-on-error: true
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "💰 UNIVERSAL REVENUE ORCHESTRATOR - THE MASTER CYCLE"
          echo "═══════════════════════════════════════════════════════════════"
          
          echo "1. Run full revenue cycle..."
          curl -s -X POST "${{ env.BACKEND_URL }}/revenue-orchestrator/run-cycle" --max-time 300 | jq -c '{ok,cycle_id}' || true
          
          echo "2. Dashboard..."
          curl -s "${{ env.BACKEND_URL }}/revenue-orchestrator/dashboard" | jq -c '{ok,cycles_run}' || true
          
          echo "3. Revenue Summary..."
          curl -s "${{ env.BACKEND_URL }}/revenue-orchestrator/revenue-summary?hours=24" | jq -c '{ok,total_revenue}' || true
          
          echo "4. Systems..."
          curl -s "${{ env.BACKEND_URL }}/revenue-orchestrator/systems" | jq -c '{ok}' || true
          
          echo "5. Fiverr Orders..."
          curl -s -X POST "${{ env.BACKEND_URL }}/revenue-orchestrator/fiverr/process-orders" --max-time 120 | jq -c '{ok}' || true
          
          echo "6. Cart Recovery..."
          curl -s -X POST "${{ env.BACKEND_URL }}/revenue-orchestrator/cart-recovery" --max-time 60 | jq -c '{ok}' || true
          
          echo "7. Social Post Spawns..."
          curl -s -X POST "${{ env.BACKEND_URL }}/revenue-orchestrator/social/post-spawns" --max-time 120 | jq -c '{ok}' || true
          
          echo "8. Arbitrage Find..."
          curl -s -X POST "${{ env.BACKEND_URL }}/revenue-orchestrator/arbitrage/find" --max-time 120 | jq -c '{ok}' || true
          
          echo "9. MRR..."
          curl -s "${{ env.BACKEND_URL }}/revenue-orchestrator/subscriptions/mrr" | jq -c '{ok}' || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 0B: MASTER ORCHESTRATOR (Legacy)
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🎯 Master Orchestrator (2 endpoints)
        continue-on-error: true
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🎯 MASTER ORCHESTRATOR"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s -X POST "${{ env.BACKEND_URL }}/orchestrator/full-cycle" --max-time 300 | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/orchestrator/status" --max-time 30 | jq -c '{ok}' || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 1: HEALTH CHECKS
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🔑 Health Checks (7 endpoints)
        continue-on-error: true
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🔑 HEALTH CHECKS"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s "${{ env.BACKEND_URL }}/autonomous/v90/health" | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/api/health" | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/health" | jq -c '{status}' || true
          curl -s "${{ env.BACKEND_URL }}/learning/health" | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/execution/health" | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/execution/health/quick" | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/execution/health/detail" | jq -c '{ok}' || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 2: STATISTICS
      # ═══════════════════════════════════════════════════════════════════════
      - name: 📊 Statistics (4 endpoints)
        continue-on-error: true
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "📊 STATISTICS"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s "${{ env.BACKEND_URL }}/execution/stats" | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/autonomous/stats" | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/autonomous/approval-queue" | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/arbitrage/stats" | jq -c '{ok}' || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 3: APEX SYSTEMS
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🎯 APEX Systems (3 endpoints)
        continue-on-error: true
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🎯 APEX SYSTEMS"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s "${{ env.BACKEND_URL }}/apex/upgrades/dashboard" | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/apex/upgrades/verify/deliverable" -d '{}' --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/apex/upgrades/pricing/recommend" -d '{}' --max-time 60 | jq -c '{ok}' || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 4: DISCOVERY
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🔍 Discovery (11 endpoints)
        continue-on-error: true
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🔍 DISCOVERY"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s -X POST "${{ env.BACKEND_URL }}/execution/mega-discover" -d '{"enable_filters":true}' --max-time 300 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/autonomous/discover-and-execute" -d '{"auto_approve_user":true}' --max-time 180 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/autonomous/discover-and-queue" -d '{"min_win_probability":0.7}' --max-time 120 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/execution/discover-and-route" --max-time 120 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/autonomous/full-cycle" --max-time 180 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/discovery/scrape-all" --max-time 180 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/discovery/perplexity-opportunities" -d '{"category":"AI"}' --max-time 120 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/discovery/alpha" --max-time 120 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/pain-points/detect" --max-time 120 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/discovery/flow-arbitrage" --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/signals/ingest" --max-time 60 | jq -c '{ok}' || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 5: WADE + CLIENT DEALS
      # ═══════════════════════════════════════════════════════════════════════
      - name: 👤 Wade + Client Deals (9 endpoints)
        continue-on-error: true
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "👤 WADE + CLIENT DEALS"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s -X POST "${{ env.BACKEND_URL }}/wade/discover-and-queue" --max-time 120 | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/wade/fulfillment-queue" | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/services/catalog" | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/deals/stats" | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/deals/pending" | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/deals/in-progress" | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/deals/awaiting-approval" | jq -c '{ok}' || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 6: AI GENERATION
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🤖 AI Generation (5 endpoints)
        continue-on-error: true
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🤖 AI GENERATION"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s -X POST "${{ env.BACKEND_URL }}/intelligence/collect" --max-time 300 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/ai/orchestrate" --max-time 300 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/graphics/batch-generate" --max-time 300 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/audio/batch-generate" --max-time 300 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/video/batch-generate" --max-time 300 | jq -c '{ok}' || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 7: FINANCIAL
      # ═══════════════════════════════════════════════════════════════════════
      - name: 💰 Financial (10 endpoints)
        continue-on-error: true
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "💰 FINANCIAL"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s -X POST "${{ env.BACKEND_URL }}/arbitrage/run-cycle" -d '{"max_opportunities":5}' --max-time 120 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/arbitrage/execute" -d '{"max_opportunities":10}' --max-time 120 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/revenue/reconcile" --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/pricing/optimize" --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/ocl/auto-repay" --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/p2p/match-loans" --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/escrow/auto-release" --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/payments/batch-execute" --max-time 120 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/ipvault/royalty-sweep" --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/subscriptions/process-renewals" --max-time 60 | jq -c '{ok}' || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 8: MARKETING
      # ═══════════════════════════════════════════════════════════════════════
      - name: 📣 Marketing (8 endpoints)
        continue-on-error: true
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "📣 MARKETING"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s -X POST "${{ env.BACKEND_URL }}/email/send-batch" --max-time 120 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/resend/process-queue" --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/ame/process-queue" --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/ame/generate-pitches" --max-time 120 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/social/process-queue" --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/syndication/process" --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/retarget/process-queue" --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/actionize/deploy" --max-time 60 | jq -c '{ok}' || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 9: PLATFORM + AAM
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🎨 Platform + AAM (6 endpoints)
        continue-on-error: true
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🎨 PLATFORM + AAM"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s -X POST "${{ env.BACKEND_URL }}/fiverr/process-orders" --max-time 120 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/dribbble/post-daily" --max-time 120 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/99designs/scan-and-enter" --max-time 120 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/aam/run/shopify/shopify-abandon-v1" --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/aam/run/shopify/shopify-growth-v1" --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/aam/run/amazon/amazon-cart-nudge-v1" --max-time 60 | jq -c '{ok}' || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 10: META + SPECIAL
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🌉 Meta + Special (9 endpoints)
        continue-on-error: true
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🌉 META + SPECIAL"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s -X POST "${{ env.BACKEND_URL }}/r3/autopilot/execute-all" --max-time 120 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/proposals/auto-nudge" --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/proposals/autoclose" --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/bundles/process-sales" --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/metabridge/batch_execute" -d '{}' --max-time 120 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/hive/distribute" --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/monetization/third-party" --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/darkpool/match" --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/sponsors/distribute" --max-time 60 | jq -c '{ok}' || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 11: CONDUCTOR
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🎯 Conductor (8 endpoints)
        continue-on-error: true
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🎯 CONDUCTOR"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s -X POST "${{ env.BACKEND_URL }}/csuite/analyze-all" --max-time 120 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/week2/execute" --max-time 120 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/conductor/run-cycle" --max-time 120 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/conductor/scan-all-devices" --max-time 120 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/conductor/create-plans" --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/conductor/execute-approved" --max-time 120 | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/conductor/dashboard-all" | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/conductor/route-tasks" --max-time 120 | jq -c '{ok}' || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 12: LEARNING + RECONCILIATION
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🧠 Learning + Reconciliation (7 endpoints)
        continue-on-error: true
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🧠 LEARNING + RECONCILIATION"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s -X POST "${{ env.BACKEND_URL }}/learning/process-outcomes" --max-time 120 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/analytics/daily-snapshot" --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/reports/revenue" --max-time 60 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/reconciliation/load" --max-time 30 | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/reconciliation/dashboard" --max-time 30 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/reconciliation/persist" --max-time 30 | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/wade/dashboard" --max-time 30 | jq -c '{ok}' || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 13: AUTO-SPAWN + NETWORK
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🏭 Auto-Spawn + Network (16 endpoints)
        continue-on-error: true
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🏭 AUTO-SPAWN + NETWORK"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s -X POST "${{ env.BACKEND_URL }}/spawn/detect-trends" --max-time 120 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/spawn/run-cycle" --max-time 300 | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/spawn/dashboard" | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/spawn/businesses?status=live" | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/spawn/businesses?status=scaling" | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/spawn/lifecycle-check" --max-time 60 | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/spawn/adoptable" | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/spawn/templates" | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/spawn/network/stats" | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/spawn/network/generate-promos" --max-time 60 | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/spawn/network/audience-pool" | jq -c '{ok}' || true
          curl -s "${{ env.BACKEND_URL }}/spawn/network/recommendations" | jq -c '{ok}' || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 14: APEX UPGRADES (12 Revenue Optimization Modules)
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🚀 Apex Upgrades (12 endpoints)
        continue-on-error: true
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🚀 APEX UPGRADES - REVENUE OPTIMIZATION"
          echo "═══════════════════════════════════════════════════════════════"
          
          echo "════ UPSELL / RECOVERY ════"
          curl -s -X POST "${{ env.BACKEND_URL }}/upsell/recommend" -H "Content-Type: application/json" -d '{"order_id":"seed","sku":"logo-basic"}' --max-time 30 | jq -c '{ok,metrics}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/upsell/deploy" -H "Content-Type: application/json" -d '{"order_id":"seed"}' --max-time 30 | jq -c '{ok}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/recovery/compose" -H "Content-Type: application/json" -d '{"order_ref":"seed","cart_value":150}' --max-time 30 | jq -c '{ok,metrics}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/recovery/send" -H "Content-Type: application/json" -d '{"order_ref":"seed"}' --max-time 30 | jq -c '{ok}' || true
          
          echo "════ ROUTER / CAC PRICING ════"
          curl -s -X POST "${{ env.BACKEND_URL }}/router/score-intent" -H "Content-Type: application/json" -d '{"intent_id":"hourly_scan","signal":{"budget":300,"deadline_days":3,"niche":"graphics","channel":"fiverr"}}' --max-time 30 | jq -c '{ok,ev_usd,win_p,route_recommendation}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/router/route" -H "Content-Type: application/json" -d '{"min_ev_usd":50}' --max-time 30 | jq -c '{ok,metrics}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/pricing/cac-aware" -H "Content-Type: application/json" -d '{"base_price":250,"llm_tokens":1800000,"image_jobs":6,"margin_target":0.55}' --max-time 30 | jq -c '{ok,bid}' || true
          
          echo "════ LTV / PORTFOLIO ════"
          curl -s -X POST "${{ env.BACKEND_URL }}/ltv/predict" -H "Content-Type: application/json" -d '{"client_id":"scan","niche":"agency"}' --max-time 30 | jq -c '{ok,metrics}' || true
          curl -s "${{ env.BACKEND_URL }}/portfolio/performance" --max-time 30 | jq -c '{ok,metrics}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/portfolio/rebalance" -H "Content-Type: application/json" -d '{"auto":true}' --max-time 30 | jq -c '{ok,metrics}' || true
          
          echo "════ CHURN / PROOF ════"
          curl -s -X POST "${{ env.BACKEND_URL }}/subscriptions/churn-risk" -H "Content-Type: application/json" -d '{"sub_id":"scan","days_since_login":7}' --max-time 30 | jq -c '{ok,metrics}' || true
          curl -s -X POST "${{ env.BACKEND_URL }}/proof/badge/mint" -H "Content-Type: application/json" -d '{"deal_id":"scan"}' --max-time 30 | jq -c '{ok,metrics}' || true
          
          echo "════ CIRCUIT BREAKERS ════"
          curl -s "${{ env.BACKEND_URL }}/orchestrator/circuits" --max-time 30 | jq -c '{ok,metrics}' || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 15: WADE WORKFLOWS
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🤖 Wade Workflows (5 endpoints)
        continue-on-error: true
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🤖 WADE WORKFLOWS - AUTO APPROVE & EXECUTE"
          echo "═══════════════════════════════════════════════════════════════"
          
          echo "1. Get Wade Dashboard..."
          curl -s "${{ env.BACKEND_URL }}/wade/dashboard" --max-time 30 | jq -c '{ok,pending_approval,active_workflows}' || true
          
          echo "2. Get Execution Status..."
          curl -s "${{ env.BACKEND_URL }}/wade/execution-status" --max-time 30 | jq -c '{ok,total_workflows,stage_counts}' || true
          
          echo "3. Auto-Approve All Pending..."
          curl -s -X POST "${{ env.BACKEND_URL }}/wade/approve-all" -H "Content-Type: application/json" --max-time 60 | jq -c '{ok,approved_count}' || true
          
          echo "4. Execute Approved Workflows..."
          curl -s -X POST "${{ env.BACKEND_URL }}/wade/execute-approved" -H "Content-Type: application/json" --max-time 120 | jq -c '{ok}' || true
          
          echo "5. Process Discoveries..."
          curl -s -X POST "${{ env.BACKEND_URL }}/wade/process-discoveries" -H "Content-Type: application/json" -d '{"opportunities":[]}' --max-time 60 | jq -c '{ok}' || true

      # ═══════════════════════════════════════════════════════════════════════
      # SUMMARY
      # ═══════════════════════════════════════════════════════════════════════
      - name: 📝 Summary
        if: always()
        run: |
          echo ""
          echo "═══════════════════════════════════════════════════════════════"
          echo "📝 AIGENTSY v93 - APEX UPGRADES + WADE AUTO-APPROVE"
          echo "═══════════════════════════════════════════════════════════════"
          echo "🕐 $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo ""
          echo "🆕 v93: APEX UPGRADES (12 modules)"
          echo "  📈 /router/score-intent - EV-based routing"
          echo "  💰 /pricing/cac-aware - Margin-safe bidding"
          echo "  🛒 /upsell/recommend - Bundle optimization"
          echo "  🔄 /recovery/compose - Cart recovery"
          echo "  📊 /ltv/predict - Client prioritization"
          echo "  ⚖️ /portfolio/rebalance - Offer mix tuning"
          echo "  🛡️ /subscriptions/churn-risk - MRR protection"
          echo "  🏆 /proof/badge/mint - Social proof"
          echo ""
          echo "🆕 v93: WADE AUTO-APPROVE"
          echo "  ✅ /wade/approve-all - Bulk approve"
          echo "  📋 /wade/execution-status - Live status"
          echo ""
          echo "TOTAL: 152 endpoints across 15 phases"
          echo "═══════════════════════════════════════════════════════════════"
