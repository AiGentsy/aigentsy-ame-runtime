# ═══════════════════════════════════════════════════════════════════════════════
# AiGentsy Autonomous Execution Workflow v7 - ALL APIs LIVE
# Updated: January 3, 2026
# ═══════════════════════════════════════════════════════════════════════════════
#
# ALL YOUR API KEYS NOW ACTUALLY USED:
#   ✅ RESEND_API_KEY - Real email sending
#   ✅ STABILITY_API_KEY - Real image generation  
#   ✅ OPENROUTER_API_KEY - Multi-AI routing (Claude, GPT, Llama)
#   ✅ PERPLEXITY_API_KEY - Real-time search
#   ✅ GEMINI_API_KEY - Google AI
#   ✅ RUNWAY_API_KEY - Video generation
#   ✅ ELEVENLABS_API_KEY - Audio/voice generation
#   ✅ GITHUB_TOKEN - Real GitHub/Reddit/HN scraping
#   ✅ SHOPIFY_ADMIN_TOKEN - Store management
#
# Plus all v6 systems still running
#
# ═══════════════════════════════════════════════════════════════════════════════

name: AiGentsy Autonomous Execution v7

on:
  schedule:
    - cron: '0 * * * *'  # Every hour
  
  workflow_dispatch:
    inputs:
      mode:
        description: 'Execution mode'
        required: false
        default: 'full'
        type: choice
        options:
          - full
          - discovery_only
          - platforms_only
          - ai_only
          - email_only
          - health_only

env:
  BACKEND_URL: ${{ secrets.BACKEND_URL || 'https://aigentsy-ame-runtime.onrender.com' }}

jobs:
  autonomous:
    name: 🚀 v7 All APIs Live
    runs-on: ubuntu-latest
    timeout-minutes: 45
    
    steps:
      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 1: API HEALTH CHECK
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🔑 API Health Check
        id: health
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🔑 API HEALTH CHECK - All 10 APIs"
          echo "═══════════════════════════════════════════════════════════════"
          
          RESPONSE=$(curl -s --max-time 30 "${{ env.BACKEND_URL }}/api/health" 2>&1 || echo '{"ok": false}')
          
          if echo "$RESPONSE" | jq -e '.ok == true' >/dev/null 2>&1; then
            CONFIGURED=$(echo "$RESPONSE" | jq -r '.apis_configured // 0')
            TOTAL=$(echo "$RESPONSE" | jq -r '.apis_total // 0')
            echo "✅ APIs Configured: $CONFIGURED / $TOTAL"
            echo ""
            echo "$RESPONSE" | jq '.apis' 2>/dev/null || true
          else
            echo "⚠️ Backend starting up, checking basic health..."
            curl -s "${{ env.BACKEND_URL }}/health" | jq . || true
            sleep 20
          fi

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 2: REAL PLATFORM SCRAPING (GitHub, Reddit, HackerNews)
      # Uses: GITHUB_TOKEN
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🔍 Real Platform Scraping
        continue-on-error: true
        if: ${{ github.event.inputs.mode != 'platforms_only' && github.event.inputs.mode != 'email_only' && github.event.inputs.mode != 'health_only' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🔍 REAL SCRAPING - GitHub, Reddit, HackerNews"
          echo "   Using: GITHUB_TOKEN for higher rate limits"
          echo "═══════════════════════════════════════════════════════════════"
          
          RESPONSE=$(curl -s -X POST "${{ env.BACKEND_URL }}/discovery/scrape-all" \
            -H "Content-Type: application/json" \
            --max-time 180 2>&1 || echo '{"ok": false}')
          
          if echo "$RESPONSE" | jq -e '.ok == true' >/dev/null 2>&1; then
            TOTAL=$(echo "$RESPONSE" | jq -r '.total_opportunities // 0')
            echo "✅ Found $TOTAL REAL opportunities"
            echo ""
            echo "By Platform:"
            echo "$RESPONSE" | jq '.by_platform' 2>/dev/null || true
          else
            echo "⚠️ Real scraping not available, trying mega-discover..."
            curl -s -X POST "${{ env.BACKEND_URL }}/execution/mega-discover" \
              -H "Content-Type: application/json" \
              -d '{"enable_filters": true, "max_age_days": 30, "min_win_probability": 0.5}' \
              --max-time 300 | jq . || true
          fi

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 3: PERPLEXITY REAL-TIME SEARCH
      # Uses: PERPLEXITY_API_KEY
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🔎 Perplexity Opportunity Search
        continue-on-error: true
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == 'discovery_only' || github.event.inputs.mode == 'ai_only' || github.event.inputs.mode == '' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🔎 PERPLEXITY - Real-Time Market Intelligence"
          echo "   Using: PERPLEXITY_API_KEY"
          echo "═══════════════════════════════════════════════════════════════"
          
          RESPONSE=$(curl -s -X POST "${{ env.BACKEND_URL }}/discovery/perplexity-opportunities" \
            -H "Content-Type: application/json" \
            -d '{"category": "AI automation freelance"}' \
            --max-time 120 2>&1 || echo '{"ok": false}')
          
          if echo "$RESPONSE" | jq -e '.ok == true' >/dev/null 2>&1; then
            COUNT=$(echo "$RESPONSE" | jq -r '.opportunities_found // 0')
            echo "✅ Perplexity found $COUNT opportunity batches"
          else
            echo "⚠️ Perplexity search not available"
          fi

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 4: SIGNAL INGESTION (PREDICTIVE)
      # ═══════════════════════════════════════════════════════════════════════
      - name: 📡 Signal Ingestion
        continue-on-error: true
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == 'discovery_only' || github.event.inputs.mode == '' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "📡 SIGNAL INGESTION - Predictive Opportunities"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s -X POST "${{ env.BACKEND_URL }}/signals/ingest" \
            -H "Content-Type: application/json" \
            --max-time 120 | jq . || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 5: CROSS-AI INTELLIGENCE COLLECTION
      # Uses: OPENROUTER_API_KEY, PERPLEXITY_API_KEY, GEMINI_API_KEY
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🧠 Cross-AI Intelligence Collection
        continue-on-error: true
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == 'ai_only' || github.event.inputs.mode == '' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🧠 CROSS-AI INTELLIGENCE - All AIs Teaching Each Other"
          echo "   Using: OPENROUTER, PERPLEXITY, GEMINI"
          echo "═══════════════════════════════════════════════════════════════"
          echo ""
          echo "This is AiGentsy's novel concept:"
          echo "  • OpenRouter (Claude/GPT) analyzes patterns"
          echo "  • Perplexity provides real-time data"
          echo "  • Gemini adds alternative perspective"
          echo "  • ALL insights feed into MetaHive"
          echo ""
          
          RESPONSE=$(curl -s -X POST "${{ env.BACKEND_URL }}/intelligence/collect" \
            -H "Content-Type: application/json" \
            --max-time 300 2>&1 || echo '{"ok": false}')
          
          if echo "$RESPONSE" | jq -e '.ok == true' >/dev/null 2>&1; then
            INSIGHTS=$(echo "$RESPONSE" | jq -r '.total_insights // 0')
            echo "✅ Collected $INSIGHTS insights from AI sources"
            echo ""
            echo "By Source:"
            echo "$RESPONSE" | jq '.by_source' 2>/dev/null || true
          else
            echo "⚠️ Intelligence collection not available"
          fi

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 6: AI TASK ORCHESTRATION
      # Uses: OPENROUTER_API_KEY
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🤖 AI Task Orchestration
        continue-on-error: true
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == 'ai_only' || github.event.inputs.mode == '' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🤖 AI ORCHESTRATION - Processing Pending AI Tasks"
          echo "   Using: OPENROUTER_API_KEY (Claude, GPT, Llama)"
          echo "═══════════════════════════════════════════════════════════════"
          
          RESPONSE=$(curl -s -X POST "${{ env.BACKEND_URL }}/ai/orchestrate" \
            -H "Content-Type: application/json" \
            --max-time 300 2>&1 || echo '{"ok": false}')
          
          if echo "$RESPONSE" | jq -e '.ok == true' >/dev/null 2>&1; then
            COMPLETED=$(echo "$RESPONSE" | jq -r '.tasks_completed // 0')
            FAILED=$(echo "$RESPONSE" | jq -r '.tasks_failed // 0')
            echo "✅ Completed: $COMPLETED AI tasks"
            if [ "$FAILED" -gt 0 ]; then echo "❌ Failed: $FAILED tasks"; fi
          else
            echo "⚠️ AI orchestration not available"
          fi

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 7: GRAPHICS GENERATION
      # Uses: STABILITY_API_KEY
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🎨 Graphics Generation (Stability AI)
        continue-on-error: true
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == '' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🎨 GRAPHICS - Stability AI Image Generation"
          echo "   Using: STABILITY_API_KEY"
          echo "═══════════════════════════════════════════════════════════════"
          
          RESPONSE=$(curl -s -X POST "${{ env.BACKEND_URL }}/graphics/batch-generate" \
            -H "Content-Type: application/json" \
            --max-time 300 2>&1 || echo '{"ok": false}')
          
          if echo "$RESPONSE" | jq -e '.ok == true' >/dev/null 2>&1; then
            GENERATED=$(echo "$RESPONSE" | jq -r '.images_generated // 0')
            echo "✅ Generated $GENERATED images via Stability AI"
          else
            echo "⚠️ Graphics generation not available (no pending requests)"
          fi

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 8: AUDIO GENERATION
      # Uses: ELEVENLABS_API_KEY
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🔊 Audio Generation (ElevenLabs)
        continue-on-error: true
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == '' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🔊 AUDIO - ElevenLabs Voice Generation"
          echo "   Using: ELEVENLABS_API_KEY"
          echo "═══════════════════════════════════════════════════════════════"
          
          RESPONSE=$(curl -s -X POST "${{ env.BACKEND_URL }}/audio/batch-generate" \
            -H "Content-Type: application/json" \
            --max-time 300 2>&1 || echo '{"ok": false}')
          
          if echo "$RESPONSE" | jq -e '.ok == true' >/dev/null 2>&1; then
            GENERATED=$(echo "$RESPONSE" | jq -r '.audio_generated // 0')
            echo "✅ Generated $GENERATED audio files via ElevenLabs"
          else
            echo "⚠️ Audio generation not available (no pending requests)"
          fi

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 9: VIDEO GENERATION
      # Uses: RUNWAY_API_KEY
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🎬 Video Generation (Runway)
        continue-on-error: true
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == '' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🎬 VIDEO - Runway AI Generation"
          echo "   Using: RUNWAY_API_KEY"
          echo "═══════════════════════════════════════════════════════════════"
          
          RESPONSE=$(curl -s -X POST "${{ env.BACKEND_URL }}/video/batch-generate" \
            -H "Content-Type: application/json" \
            --max-time 300 2>&1 || echo '{"ok": false}')
          
          if echo "$RESPONSE" | jq -e '.ok == true' >/dev/null 2>&1; then
            GENERATED=$(echo "$RESPONSE" | jq -r '.videos_generated // 0')
            echo "✅ Generated $GENERATED videos via Runway"
          else
            echo "⚠️ Video generation not available (no pending requests)"
          fi

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 10: EMAIL AUTOMATION
      # Uses: RESEND_API_KEY
      # ═══════════════════════════════════════════════════════════════════════
      - name: 📧 Email Automation (Resend)
        continue-on-error: true
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == 'email_only' || github.event.inputs.mode == '' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "📧 EMAIL - Resend API"
          echo "   Using: RESEND_API_KEY"
          echo "═══════════════════════════════════════════════════════════════"
          
          RESPONSE=$(curl -s -X POST "${{ env.BACKEND_URL }}/email/send-batch" \
            -H "Content-Type: application/json" \
            --max-time 120 2>&1 || echo '{"ok": false}')
          
          if echo "$RESPONSE" | jq -e '.ok == true' >/dev/null 2>&1; then
            SENT=$(echo "$RESPONSE" | jq -r '.emails_sent // 0')
            echo "✅ Sent: $SENT emails via Resend"
          else
            echo "⚠️ Email automation not available (no pending emails)"
          fi

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 11: PLATFORM AUTOMATIONS
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🎨 Fiverr Order Processing
        continue-on-error: true
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == 'platforms_only' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🎨 FIVERR AUTOMATION"
          echo "═══════════════════════════════════════════════════════════════"
          
          RESPONSE=$(curl -s -X POST "${{ env.BACKEND_URL }}/fiverr/process-orders" \
            -H "Content-Type: application/json" \
            --max-time 120 2>&1 || echo '{"ok": false}')
          
          if echo "$RESPONSE" | jq -e '.ok == true' >/dev/null 2>&1; then
            PROCESSED=$(echo "$RESPONSE" | jq -r '.orders_processed // 0')
            echo "✅ Fiverr: Processed $PROCESSED orders"
          else
            echo "⚠️ Fiverr automation not available"
          fi

      - name: 🖼️ Dribbble Daily Posting
        continue-on-error: true
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == 'platforms_only' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🖼️ DRIBBBLE AUTOMATION"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s -X POST "${{ env.BACKEND_URL }}/dribbble/post-daily" \
            -H "Content-Type: application/json" \
            --max-time 120 | jq . || true

      - name: 🏆 99designs Contest Entry
        continue-on-error: true
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == 'platforms_only' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🏆 99DESIGNS AUTOMATION"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s -X POST "${{ env.BACKEND_URL }}/99designs/scan-and-enter" \
            -H "Content-Type: application/json" \
            --max-time 120 | jq . || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 12: AAM & SHOPIFY
      # Uses: SHOPIFY_ADMIN_TOKEN
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🛒 AAM Cart Abandonment
        continue-on-error: true
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == '' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🛒 AAM - Cart Abandonment Recovery"
          echo "   Using: SHOPIFY_ADMIN_TOKEN"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s -X POST "${{ env.BACKEND_URL }}/aam/run/shopify/shopify-abandon-v1" \
            -H "Content-Type: application/json" \
            --max-time 60 | jq . || true

      - name: 📈 Shopify Growth
        continue-on-error: true
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == '' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "📈 SHOPIFY GROWTH AUTOMATION"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s -X POST "${{ env.BACKEND_URL }}/aam/run/shopify/shopify-growth-v1" \
            -H "Content-Type: application/json" \
            --max-time 60 | jq . || true
          
          curl -s -X POST "${{ env.BACKEND_URL }}/aam/run/amazon/amazon-cart-nudge-v1" \
            -H "Content-Type: application/json" \
            --max-time 60 | jq . || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 13: R3 AUTOPILOT
      # ═══════════════════════════════════════════════════════════════════════
      - name: 📊 R3 Autopilot Budget Execution
        continue-on-error: true
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == '' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "📊 R3 AUTOPILOT - Budget Allocation"
          echo "═══════════════════════════════════════════════════════════════"
          
          RESPONSE=$(curl -s -X POST "${{ env.BACKEND_URL }}/r3/autopilot/execute-all" \
            -H "Content-Type: application/json" \
            --max-time 120 2>&1 || echo '{"ok": false}')
          
          if echo "$RESPONSE" | jq -e '.ok == true' >/dev/null 2>&1; then
            SPENT=$(echo "$RESPONSE" | jq -r '.total_spent // 0')
            echo "✅ R3 spent: \$$SPENT"
          else
            echo "⚠️ R3 autopilot not available"
          fi

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 14: RETARGETING & EMAIL SEQUENCES
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🎯 Retargeting & Email Sequences
        continue-on-error: true
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == '' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🎯 RETARGETING & EMAIL SEQUENCES"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s -X POST "${{ env.BACKEND_URL }}/retarget/process-queue" \
            -H "Content-Type: application/json" \
            --max-time 60 | jq . || true
          
          curl -s -X POST "${{ env.BACKEND_URL }}/resend/process-queue" \
            -H "Content-Type: application/json" \
            --max-time 60 | jq . || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 15: AME & SOCIAL
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🎯 AME Pitch Processing
        continue-on-error: true
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == '' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🎯 AME - Autonomous Marketing Engine"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s -X POST "${{ env.BACKEND_URL }}/ame/process-queue" \
            -H "Content-Type: application/json" \
            --max-time 60 | jq . || true

      - name: 📱 Social Auto-Posting
        continue-on-error: true
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == '' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "📱 SOCIAL MEDIA AUTOMATION"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s -X POST "${{ env.BACKEND_URL }}/social/process-queue" \
            -H "Content-Type: application/json" \
            --max-time 60 | jq . || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 16: PROPOSALS
      # ═══════════════════════════════════════════════════════════════════════
      - name: 📝 Proposal Lifecycle
        continue-on-error: true
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == '' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "📝 PROPOSAL MANAGEMENT"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s -X POST "${{ env.BACKEND_URL }}/proposals/auto-nudge" \
            -H "Content-Type: application/json" \
            --max-time 60 | jq . || true
          
          curl -s -X POST "${{ env.BACKEND_URL }}/proposals/autoclose" \
            -H "Content-Type: application/json" \
            --max-time 60 | jq . || true

      - name: 📦 Bundle Sales Processing
        continue-on-error: true
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == '' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "📦 BUNDLE ENGINE"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s -X POST "${{ env.BACKEND_URL }}/bundles/process-sales" \
            -H "Content-Type: application/json" \
            --max-time 60 | jq . || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 17: METABRIDGE & METAHIVE
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🌉 MetaBridge Team Formation
        continue-on-error: true
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == '' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🌉 METABRIDGE - Auto-Assemble JV Teams"
          echo "═══════════════════════════════════════════════════════════════"
          
          RESPONSE=$(curl -s -X POST "${{ env.BACKEND_URL }}/metabridge/batch_execute" \
            -H "Content-Type: application/json" \
            -d '{"intent_ids": []}' \
            --max-time 120 2>&1 || echo '{"ok": false}')
          
          if echo "$RESPONSE" | jq -e '.ok == true' >/dev/null 2>&1; then
            TEAMS=$(echo "$RESPONSE" | jq -r '.teams_formed // 0')
            echo "✅ MetaBridge: $TEAMS teams formed"
          else
            echo "⚠️ MetaBridge not available"
          fi

      - name: 🐝 MetaHive Reward Distribution
        continue-on-error: true
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == '' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🐝 METAHIVE - Distribute Learning Rewards"
          echo "═══════════════════════════════════════════════════════════════"
          
          curl -s -X POST "${{ env.BACKEND_URL }}/hive/distribute" \
            -H "Content-Type: application/json" \
            --max-time 60 | jq . || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 18: CROSS-AI LEARNING LOOP
      # ═══════════════════════════════════════════════════════════════════════
      - name: 🧠 Cross-AI Learning Loop
        continue-on-error: true
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == '' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "🧠 CROSS-AI LEARNING - All AIs Teaching Each Other"
          echo "═══════════════════════════════════════════════════════════════"
          echo ""
          echo "This is AiGentsy's novel concept:"
          echo "  • Claude finds winning patterns"
          echo "  • Contributes to MetaHive"
          echo "  • ChatGPT/Stability/Perplexity use patterns"
          echo "  • All AIs benefit from collective intelligence"
          echo ""
          
          RESPONSE=$(curl -s -X POST "${{ env.BACKEND_URL }}/learning/process-outcomes" \
            -H "Content-Type: application/json" \
            --max-time 120 2>&1 || echo '{"ok": false}')
          
          if echo "$RESPONSE" | jq -e '.ok == true' >/dev/null 2>&1; then
            PATTERNS=$(echo "$RESPONSE" | jq -r '.patterns_stored // 0')
            HIVE=$(echo "$RESPONSE" | jq -r '.hive_contributions // 0')
            echo "✅ Yield Memory: $PATTERNS patterns stored"
            echo "✅ MetaHive: $HIVE patterns contributed"
          else
            echo "⚠️ Learning loop not available"
          fi
          
          # Check learning health
          curl -s -X GET "${{ env.BACKEND_URL }}/learning/health" --max-time 30 | jq . || true

      # ═══════════════════════════════════════════════════════════════════════
      # PHASE 19: ARBITRAGE
      # ═══════════════════════════════════════════════════════════════════════
      - name: 💵 Arbitrage Execution
        continue-on-error: true
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == '' }}
        run: |
          echo "═══════════════════════════════════════════════════════════════"
          echo "💵 ARBITRAGE - Cross-Platform Price/Temporal/Supply"
          echo "═══════════════════════════════════════════════════════════════"
          
          RESPONSE=$(curl -s -X POST "${{ env.BACKEND_URL }}/arbitrage/execute" \
            -H "Content-Type: application/json" \
            -d '{"max_opportunities": 10}' \
            --max-time 120 2>&1 || echo '{"ok": false}')
          
          if echo "$RESPONSE" | jq -e '.ok == true' >/dev/null 2>&1; then
            PROFIT=$(echo "$RESPONSE" | jq -r '.total_profit // 0')
            echo "✅ Arbitrage profit: \$$PROFIT"
          else
            echo "⚠️ Arbitrage not available"
          fi

      # ═══════════════════════════════════════════════════════════════════════
      # SUMMARY
      # ═══════════════════════════════════════════════════════════════════════
      - name: 📝 Run Summary
        if: always()
        run: |
          echo ""
          echo "═══════════════════════════════════════════════════════════════"
          echo "📝 AIGENTSY v7 - ALL APIS LIVE - COMPLETE"
          echo "═══════════════════════════════════════════════════════════════"
          echo ""
          echo "🕐 Time: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "🔧 Mode: ${{ github.event.inputs.mode || 'full' }}"
          echo "🔗 Backend: ${{ env.BACKEND_URL }}"
          echo ""
          echo "🔑 APIS USED THIS RUN:"
          echo "   📧 RESEND_API_KEY - Email sending"
          echo "   🎨 STABILITY_API_KEY - Image generation"
          echo "   🔊 ELEVENLABS_API_KEY - Audio/voice generation"
          echo "   🎬 RUNWAY_API_KEY - Video generation"
          echo "   🤖 OPENROUTER_API_KEY - Multi-AI (Claude/GPT)"
          echo "   🔍 PERPLEXITY_API_KEY - Real-time search"
          echo "   🧠 GEMINI_API_KEY - Google AI"
          echo "   🐙 GITHUB_TOKEN - Real scraping"
          echo "   🛒 SHOPIFY_ADMIN_TOKEN - Store management"
          echo ""
          echo "📊 SYSTEMS EXECUTED:"
          echo "   ✅ Real Platform Scraping (GitHub/Reddit/HN)"
          echo "   ✅ Perplexity Opportunity Search"
          echo "   ✅ Signal Ingestion"
          echo "   ✅ Cross-AI Intelligence Collection"
          echo "   ✅ AI Task Orchestration"
          echo "   ✅ Graphics Generation (Stability)"
          echo "   ✅ Audio Generation (ElevenLabs)"
          echo "   ✅ Video Generation (Runway)"
          echo "   ✅ Email Automation (Resend)"
          echo "   ✅ Fiverr Automation"
          echo "   ✅ Dribbble Automation"
          echo "   ✅ 99designs Automation"
          echo "   ✅ Cart Abandonment Recovery"
          echo "   ✅ Shopify Growth"
          echo "   ✅ R3 Autopilot"
          echo "   ✅ Retargeting"
          echo "   ✅ AME Pitches"
          echo "   ✅ Social Posting"
          echo "   ✅ Proposals"
          echo "   ✅ Bundles"
          echo "   ✅ MetaBridge Team Formation"
          echo "   ✅ MetaHive Reward Distribution"
          echo "   ✅ Cross-AI Learning Loop"
          echo "   ✅ Arbitrage"
          echo ""
          echo "🧠 LEARNING SYSTEMS:"
          echo "   • Yield Memory: Per-user pattern learning"
          echo "   • MetaHive Brain: Global cross-AI patterns"
          echo "   • Open MetaHive API: External AI contributions"
          echo ""
          echo "═══════════════════════════════════════════════════════════════"
